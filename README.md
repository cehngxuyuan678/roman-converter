# roman-converter
A tiny command-line tool that converts an integer (1..=3999) into a Roman numeral.

## Run
```sh
rustc main.rs -O -o roman-converter
./roman-converter 1994
```
Prints the Roman numeral to stdout.
